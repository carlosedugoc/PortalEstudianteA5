import { GetFileNameOfPathPipe } from './get-file-name-of-path.pipe';

describe('GetFileNameOfPathPipe', () => {
  it('create an instance', () => {
    const pipe = new GetFileNameOfPathPipe();
    expect(pipe).toBeTruthy();
  });
});
